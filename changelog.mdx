---
title: Changelog
description: Track the AIGravity core monorepo as a starter kit, with dated updates tied to main branch commits and infrastructure changes.
---

## Repository status

<Callout kind="info">
AIGravity/core is a pre-1.0 monorepo starter kit. The Remix web app, NestJS API on Bun, and FastAPI AI service are under active development, and only basic health-check endpoints are documented. Each service is independently versioned through VERSION files in its directory.
</Callout>

## Recent updates

<Update label="2026-03-15" description="Minor API service change" tags={["improvement"]}>
## Changes

- Updated `apps/api/src/main.ts` with a minor test comment change to validate CI and formatting flows
- Confirmed NestJS API service continues to build and start correctly on Bun after the change

</Update>

<Update label="2026-03-07" description="CI, versioning, and container updates" tags={["feature", "improvement"]}>
## Continuous integration and workflows

- Added and refined GitHub Actions workflows for monorepo builds and checks
- Integrated version bump scripts to keep service VERSION files aligned with changes on main
- Ensured workflows run on pushes and pull requests targeting the main branch

## Containerization and tooling

- Refactored Dockerfiles across services to streamline build steps and reduce duplication
- Added a `tsconfig` copy step where needed to make container builds consistent with local development
- Merged the first feature branch through PR `#1` from `dev` into `main` as the baseline for future work

</Update>

<Update label="2026-03-01" description="Initial repository setup" tags={["feature"]}>
## Initial monorepo layout

- Created the core monorepo structure for a full-stack starter kit
- Added a Remix-based web application, a NestJS API running on Bun, and a FastAPI AI service using uv
- Defined basic health-check endpoints for services to verify wiring and deployment

## Tooling and configuration

- Initialized TypeScript configuration shared across services
- Set up base project scripts for local development and testing

</Update>

## Weekly update process

Use this process to keep the changelog aligned with work landing on the main branch.

<Steps>
<Step title="Review recent commits" icon="terminal">
List commits on the `main` branch from the last 7 days and group them by theme, such as CI, API, web, or AI service changes.
</Step>

<Step title="Summarize meaningful changes" icon="edit">
For each theme, write 2–5 concise bullets that describe what changed and why it matters at a starter-kit level, avoiding implementation detail noise.
</Step>

<Step title="Add or update an entry" icon="plus">
Create a new `<Update>` entry with the latest date and a short description, or append to the most recent entry when changes are closely related.
</Step>

<Step title="Verify scope and status" icon="check-circle">
Confirm that the entry does not claim features beyond the current implementation, and that it reflects the pre-1.0, independently versioned services model.
</Step>
</Steps>