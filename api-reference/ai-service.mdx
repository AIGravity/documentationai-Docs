---
title: AI Service
description: Verify AI service health and delete agents from the search index through the internal FastAPI AI service API.
---

## Overview

The AI service exposes a public health endpoint and an internal agent deletion endpoint. Use `GET /health` to verify that the service is running, and use `DELETE /internal/agents/{agent_id}` to remove an agent from the search index.

## Endpoint

### GET /health

Use this endpoint to verify that the AI service is running and able to respond to requests.

<ParamField body="method" param-type="string" required="true" show-location="false">

`GET`


</ParamField>

<ParamField body="path" param-type="string" required="true" show-location="false">

`/health`


</ParamField>

<ParamField body="auth" param-type="string" required="false" show-location="false">

No authentication requirement was found in the FastAPI route implementation.


</ParamField>

<CodeGroup tabs="cURL,JSON response">

```bash
curl -X GET http://localhost:8000/health
```

```json
{
  "status": "ok"
}
```

</CodeGroup>

## Request details

This endpoint does not accept any request inputs beyond the request method and path.

<ParamField body="path parameters" param-type="none" required="false" show-location="false">

None. The route path is static and does not define any path parameters.


</ParamField>

<ParamField body="query parameters" param-type="none" required="false" show-location="false">

None. No query parameters are defined in the route handler.


</ParamField>

<ParamField body="request body" param-type="none" required="false" show-location="false">

None. Send the request without a body.


</ParamField>

## Response

A successful request returns a JSON object with a single status field.

<ResponseField name="status" field-type="string" required="true">

Service health status. The current implementation returns `ok`.


</ResponseField>

<Response tabs="200 OK" show-lines="false">

```json
{
  "status": "ok"
}
```

</Response>

## Internal endpoints

### DELETE `/internal/agents/{agent_id}`

Delete an agent from the AI service search index. This is an internal endpoint and does not require authentication.

<Callout kind="info">

On failure, the `error` field is set in the response payload. Upstream callers log the error and never roll back — the caller is responsible for handling the failure.


</Callout>

<ParamField path="agent_id" param-type="string" required="true">

Agent identifier to delete from the search index.


</ParamField>

<CodeGroup tabs="cURL,JSON response">

```bash
curl -X DELETE http://localhost:8000/internal/agents/agt_7f3c2a91
```

```json
{
  "deleted": true,
  "error": null
}
```

</CodeGroup>

<ResponseField name="deleted" field-type="boolean" required="true">

Whether the agent was successfully removed from the Typesense search index.


</ResponseField>

<ResponseField name="error" field-type="string|null" required="true">

Error message when deletion fails, or `null` when deletion succeeds.


</ResponseField>

<Response tabs="200 OK" show-lines="false">

```json
{
  "deleted": true,
  "error": null
}
```

</Response>

## Implementation notes

The FastAPI file defines the application with `app = FastAPI()` and registers the `GET /health` handler directly. The AI service exposes the health endpoint and the internal agent deletion endpoint.

## Likely error responses

The current implementation is minimal, so likely error behavior is limited to framework defaults and unexpected runtime failures.

- **`404 Not Found`** — Returned when you request a path that is not defined.
- **`404 Not Found`** — Returned when you use a path or method combination that does not match the implemented route, based on the available code analysis.
- **`500 Internal Server Error`** — Possible only if the service encounters an unexpected runtime failure while handling the request.

The `DELETE /internal/agents/{agent_id}` endpoint may return `200 OK` even when deletion fails. In that case, check the `error` field in the response payload instead of relying only on the HTTP status code.